# Grafana 数据源自动置备(T0.8):默认接 Prometheus,免手动配置。
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # 固定 uid:面板 JSON 以 uid 引用数据源,置备时必须一致,否则面板加载失败
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
